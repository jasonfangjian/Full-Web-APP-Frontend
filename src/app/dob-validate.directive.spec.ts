import { DobValidateDirective } from './dob-validate.directive';

describe('DobValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new DobValidateDirective();
    expect(directive).toBeTruthy();
  });
});
